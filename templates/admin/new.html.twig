{% extends 'base.html.twig' %}

{% block title %}Créer un nouvel utilisateur{% endblock %}

{% block body %}

    <h1 class="h3 mb-3 font-weight-normal">Création d'un nouvel utilisateur</h1>
    <form method="post">
        <div class="mb-3">
            {{ form_errors(formUser) }}
        </div>

        {{ form_start(formUser) }}

        <div class="mb-3">
            <label for="inputNom" class="form-label">Nom</label>
            {{ form_widget(formUser.nom, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="mb-3">
            <label for="inputPrenom" class="form-label">Prénom</label>
            {{ form_widget(formUser.prenom, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="mb-3">
            <label for="inputEmail" class="form-label">Email</label>
            {{ form_widget(formUser.email, {'attr': {'class': 'form-control', 'autocomplete': 'email'}}) }}
        </div>

        <div class="mb-3">
            <label for="inputTelephone" class="form-label">Téléphone</label>
            {{ form_widget(formUser.telephone, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="mb-3">
            <label for="inputPassword" class="form-label">Mot de passe</label>
            {{ form_widget(formUser.password, {'attr': {'class': 'form-control', 'autocomplete': 'new-password'}}) }}
        </div>

{#        <div class="form-check mb-3">#}
{#            {{ form_widget(formUser.roles, {'label': "roles", 'attr': {'class': 'form-check-input'}}) }}#}
{#            <label class="form-check-label" for="roles">Roles</label>#}
{#        </div>#}
        <div>
            {{ form_widget(formUser.roles)}}
        </div>

{#        <div class="form-check mb-3">#}
{#            {{ form_widget(formUser.isAdmin, {'label': "isAdmin", 'attr': {'class': 'form-check-input'}}) }}#}
{#            <label class="form-check-label" for="isAdmin">Admin</label>#}
{#        </div>#}

{#        <div class="form-check mb-3">#}
{#            {{ form_widget(formUser.isActif, {'label': "isActif", 'attr': {'class': 'form-check-input'}}) }}#}
{#            <label class="form-check-label" for="isActif">Actif</label>#}
{#        </div>#}

        <button type="submit" class="btn btn-lg btn-primary">Enregistrer</button>
        {{ form_end(formUser) }}
    </form>





    <a href="{{ path('app_admin_liste') }}">back to list</a>
{% endblock %}